import React from 'react';

// Welcome page is no longer used - intro is at app-level
// "/" now redirects directly to "/home" after intro completes
function Welcome() {
  return null;
}

export default Welcome;
